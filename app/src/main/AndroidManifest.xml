<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="gasds">


  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

  <uses-permission android:name="android.permission.INTERNET" />

  <uses-permission android:name = "android.permission.SET_WALLPAPER"/>

  <application
    android:allowBackup="true"
    android:icon="@mipmap/bing_logo"
    android:requestLegacyExternalStorage="true"
    android:label="@string/app_name"
    android:name="com.example.bing.MyApp"
    android:networkSecurityConfig="@xml/network_security_config"
    android:roundIcon="@mipmap/bing_logo"
    android:supportsRtl="true"
    android:theme="@style/FullScreen">
    <activity android:name="com.example.bing.ui.activities.MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name="com.example.bing.ui.activities.HistoryActivity"/>
  </application>


</manifest>


